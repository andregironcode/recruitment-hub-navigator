// This file is automatically generated. Do not edit it directly.
import { createClient } from '@supabase/supabase-js';
import type { Database } from './types';

const SUPABASE_URL = "https://dwndivcqwkyzmjdsarws.supabase.co";
const SUPABASE_PUBLISHABLE_KEY = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImR3bmRpdmNxd2t5em1qZHNhcndzIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjIzNTU2ODYsImV4cCI6MjA3NzkzMTY4Nn0.npAYiLx_zTI-ZDPe1dPb_pwXsV2OiRQMQ0nVuG4R3ZQ";

// Import the supabase client like this:
// import { supabase } from "@/integrations/supabase/client";

export const supabase = createClient<Database>(SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY, {
  auth: {
    storage: localStorage,
    persistSession: true,
    autoRefreshToken: true,
  }
});

export const RESUME_BUCKET_ID = 'resumes';

export const checkStorageBuckets = async () => {
  try {
    const { data, error } = await supabase.storage.getBucket(RESUME_BUCKET_ID);
    
    if (error) {
      return { success: false, error, bucketInfo: null };
    }
    
    return { success: true, error: null, bucketInfo: data };
  } catch (error) {
    return { 
      success: false, 
      error: error instanceof Error ? error : new Error('Unknown error'),
      bucketInfo: null 
    };
  }
};

export const uploadResumeFile = async (file: File) => {
  try {
    const fileExt = file.name.split('.').pop();
    const fileName = `${Math.random().toString(36).substring(2)}-${Date.now()}.${fileExt}`;
    const filePath = fileName;

    const { data, error } = await supabase.storage
      .from(RESUME_BUCKET_ID)
      .upload(filePath, file, {
        cacheControl: '3600',
        upsert: false
      });

    if (error) {
      throw error;
    }

    const { data: { publicUrl } } = supabase.storage
      .from(RESUME_BUCKET_ID)
      .getPublicUrl(filePath);

    // Extract text content from file if possible
    let content = '';
    if (file.type === 'application/pdf' || file.type.includes('text')) {
      content = await file.text().catch(() => '');
    }

    return {
      url: publicUrl,
      content,
      error: null
    };
  } catch (error) {
    return {
      url: '',
      content: '',
      error: error instanceof Error ? error : new Error('Upload failed')
    };
  }
};